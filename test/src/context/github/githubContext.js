//all we are doing is initializing a new context





import { createContext } from 'react';            //might be a module for context to be bought in


const githubContext =createContext();            //the method we imported

export default githubContext;